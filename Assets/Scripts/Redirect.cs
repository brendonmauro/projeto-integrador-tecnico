using UnityEngine;
using System.Collections;

public class Redirect : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Application.LoadLevel("BuildingStage");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
